# OpenAI
OPENAI_API_KEY=sk-...
REALTIME_MODEL=gpt-4o-realtime-preview-2024-12-17
REALTIME_VOICE=alloy
PORT=4001

# GitHub – choose ONE mode below

# (A) STDIO (local binary OR docker)
GITHUB_PERSONAL_ACCESS_TOKEN=ghp_...

# Optional: if you want to restrict available toolsets (recommended)
# GITHUB_TOOLSETS="repos,pull_requests,issues"   # or "all"

# (B) Streamable HTTP (GitHub managed endpoint)
GITHUB_MCP_URL=https://api.githubcopilot.com/mcp/
GITHUB_MCP_PAT=ghp_...   # used as: Authorization: Bearer <token>

# Safety: set to true to perform write actions without verbal confirmation
AUTO_APPLY=false
